# Minimal import/inference layer for the pinned official LAMM commit.
#
# Deliberately excludes torch, torchvision and numpy.  The post-hoc notebook
# uses the Colab GPU stack already validated by Rhinoform and installs these
# packages into an isolated /content directory with --no-deps.  PyYAML 6.0.2
# is the wheel-compatible patch release of the official repository's 6.0 pin.
trimesh==3.22.3
PyYAML==6.0.2
einops==0.6.1
timm==0.9.2
